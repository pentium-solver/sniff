#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

cd "$ROOT_DIR/frontend"
bun run build

cd "$ROOT_DIR"
go build -o sniff-tui .

echo "Built: $ROOT_DIR/sniff-tui"
